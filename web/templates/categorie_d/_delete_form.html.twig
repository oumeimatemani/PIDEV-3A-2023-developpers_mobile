<form method="post" action="{{ path('app_categorie_d_delete', {'id': categorie_d.id}) }}" onsubmit="return confirm('Are you sure you want to delete this categoie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_d.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
