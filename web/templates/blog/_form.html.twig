{{ form_start(form,{'attr':{'class':'form-control','style':'height:100%'}}) }}

    {{ form_widget(form) }}
    <label class="col-sm-2 col-form-label"></label>
                  <div class="col-sm-10">
                    <button type="submit" class="btn btn-primary">Submit</button>
                  </div>
{{ form_end(form) }}