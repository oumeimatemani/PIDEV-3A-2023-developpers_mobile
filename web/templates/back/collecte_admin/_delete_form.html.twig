<form method="post" action="{{ path('app_collecte_delete_admin', {'id': collecte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collecte.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
