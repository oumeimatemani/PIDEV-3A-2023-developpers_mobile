{% extends 'base.html.twig' %}

{% block title %}Don{% endblock %}

{% block body %}
<main id="main" class="main">

<section id="about" class="about">
    <div class="container" data-aos="fade-up">
        <div class="row gy-4">

    <h1>Don</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ don.id }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ don.poids }}</td>
            </tr>
            <tr>
                <th>DescriptionD</th>
                <td>{{ don.descriptionD }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ don.etat }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ don.date ? don.date|date('Y-m-d') : '' }}</td>
            </tr>
            <th>
                <a href="{{ path('app_don_edit_admin', {'id': don.id}) }}">  <button type="submit" class="btn btn-primary">  edit</button></a>


            </th>
            <td>{{ include('back/don_admin/_delete_form.html.twig') }}</td>
        </tbody>
    </table>



    <a href="{{ path('app_don_index_admin') }}">back to list</a>



        </div></div></section></main>
{% endblock %}
